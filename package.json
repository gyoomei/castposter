{
  "name": "castposter",
  "version": "1.0.0",
  "description": "CastMint - turn Farcaster casts into NFT concepts on Base",
  "type": "module",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "rm -rf dist-test && tsc -p tsconfig.test.json && node tests/cast-nft.test.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@farcaster/miniapp-sdk": "^0.3.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^6.3.5"
  }
}
